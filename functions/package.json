{
  "name": "miresan-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "prebuild": "node ../scripts/sync-functions-shared.mjs",
    "build": "tsc -p tsconfig.json",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
